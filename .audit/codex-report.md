No critical findings. Eight high, seven medium, and one low finding.

1. [grid.ts:447](/Users/hodlmedia/agripinaa/apps/agents/src/agents/grid.ts:447), [lp-range.ts:498](/Users/hodlmedia/agripinaa/apps/agents/src/agents/lp-range.ts:498) — **high** — Slippage is calculated against the Ophis quote itself. A malicious quote can return two wei of output, producing a one-wei signed minimum while selling the full input; receiver pinning does not prevent this loss. **Fix:** require an independently derived TWAP/quorum-based minimum inside `executeOphisSwap`, before approval and signing.

2. [lp-range.ts:364](/Users/hodlmedia/agripinaa/apps/agents/src/agents/lp-range.ts:364), [lp-range.ts:425](/Users/hodlmedia/agripinaa/apps/agents/src/agents/lp-range.ts:425) — **high** — Both LP mint and liquidity removal use `amount0Min = amount1Min = 0`. An attacker can move the pool price before execution, force a one-sided mint/withdrawal, then reverse the move and arbitrage the loss. **Fix:** calculate nonzero minimums from a manipulation-resistant price with bounded slippage.

3. [chassis.ts:65](/Users/hodlmedia/agripinaa/apps/agents/src/chassis.ts:65), [health-factor.ts:366](/Users/hodlmedia/agripinaa/apps/agents/src/agents/health-factor.ts:366) — **high** — `fallback()` accepts the first successful RPC response; it is failover, not quorum. A malicious RPC can report a healthy HF while the position is liquidatable, fabricate receipts, or manipulate gas estimates. **Fix:** require agreement across independent providers at a pinned block, verify receipts independently, use TWAP/oracle sanity checks, and cap transaction fees/gas explicitly.

4. [runner.ts:51](/Users/hodlmedia/agripinaa/apps/agents/src/runner.ts:51), [chassis.ts:101](/Users/hodlmedia/agripinaa/apps/agents/src/chassis.ts:101), [start-agents.sh:6](/Users/hodlmedia/agripinaa/ops/start-agents.sh:6) — **high** — The overlap guard and action counters are process-local. Running the systemd service and local start script—or two replicas—permits duplicate Ophis orders and bypasses daily caps because each process has stale independent state. **Fix:** acquire an exclusive per-wallet lease and store counters/intents transactionally; reconcile open orders on boot.

5. [chassis.ts:19](/Users/hodlmedia/agripinaa/apps/agents/src/chassis.ts:19), [chassis.ts:33](/Users/hodlmedia/agripinaa/apps/agents/src/chassis.ts:33), [deploy-aleph.sh:26](/Users/hodlmedia/agripinaa/ops/deploy-aleph.sh:26) — **high** — Safety state is non-atomic and fail-open: any parse error becomes `{}`. Additionally, the live state files are tracked, so `git reset --hard origin/main` can erase an on-host halt and roll action counters back during redeployment. **Fix:** store runtime state outside the repository, use atomic fsync/rename with backups, and refuse to start when safety state is corrupt.

6. [chassis.ts:119](/Users/hodlmedia/agripinaa/apps/agents/src/chassis.ts:119), [reputation.ts:101](/Users/hodlmedia/agripinaa/packages/exec-metrics/src/reputation.ts:101), [runner.ts:34](/Users/hodlmedia/agripinaa/apps/agents/src/runner.ts:34) — **high** — Optional reputation enrollment has no timeout and is awaited during sequential startup. A server that accepts the connection but never responds prevents the health-factor guardian from starting, exposing the position to liquidation. **Fix:** use a short abort timeout and perform enrollment asynchronously after agents are live.

7. [deploy-aleph.sh:7](/Users/hodlmedia/agripinaa/ops/deploy-aleph.sh:7), [deploy-aleph.sh:9](/Users/hodlmedia/agripinaa/ops/deploy-aleph.sh:9), [deploy-aleph.sh:28](/Users/hodlmedia/agripinaa/ops/deploy-aleph.sh:28) — **high** — `HOST` can begin with an SSH option such as `-oProxyCommand=...`, causing local command execution; `KEY` is also interpolated into rsync’s shell-like `-e` string. **Fix:** strictly validate destination/key syntax, reject leading dashes, resolve the key to a regular file, and avoid string-built remote-shell commands.

8. [deploy-aleph.sh:14](/Users/hodlmedia/agripinaa/ops/deploy-aleph.sh:14), [deploy-aleph.sh:19](/Users/hodlmedia/agripinaa/ops/deploy-aleph.sh:19) — **high** — Mutable NodeSource and “latest” Cloudflare downloads are executed as root without checksum/signature verification. A compromised response installs persistent root malware before wallet keys are copied. **Fix:** pin versions and hashes/signatures; never pipe downloaded scripts directly into `sudo`.

9. [grid.ts:452](/Users/hodlmedia/agripinaa/apps/agents/src/agents/grid.ts:452) — **medium** — Order submission is recorded as a fill without checking settlement. After cooldown or recentering, another order—including the opposite side—can be signed while the first remains executable for 30 minutes. **Fix:** persist a pending order, reserve its sell balance, query authoritative status, and cancel/reconcile it before another trade.

10. [grid.ts:245](/Users/hodlmedia/agripinaa/apps/agents/src/agents/grid.ts:245) — **medium** — Cooldown and rate-limit checks run before trend and loss breakers. During cooldown or after exhausting the cap, a 6% breakout or 5% loss does not permanently halt the agent. **Fix:** evaluate and persist all halting conditions before non-halting guards.

11. [persist.ts:36](/Users/hodlmedia/agripinaa/packages/session-kit/src/persist.ts:36), [fund.ts:67](/Users/hodlmedia/agripinaa/apps/agents/src/fund.ts:67), [session-store.ts:35](/Users/hodlmedia/agripinaa/apps/web/src/lib/session-store.ts:35) — **medium** — Session objects contain a private signer, but disk writes use default creation modes and browser sessions are stored extractably in `localStorage`. The existing `spike-b-session.json` is mode `0644`; wallet generation also chmods only after writing. **Fix:** create files atomically with mode `0600`, rotate the exposed session, and use a non-extractable WebCrypto-backed signer in the browser.

12. [spike-b.ts:61](/Users/hodlmedia/agripinaa/packages/spikes/scripts/spike-b.ts:61), [spike-b-x402.ts:135](/Users/hodlmedia/agripinaa/packages/spikes/scripts/spike-b-x402.ts:135) — **medium** — These direct grants omit `spend.token`; the limit therefore caps native BNB, not TestUSD. A stolen session can invoke `transfer` on the allowlisted token without a stablecoin cap. **Fix:** exclusively use `buildSessionScope`, which pins the token address.

13. [cow.ts:175](/Users/hodlmedia/agripinaa/packages/exec-metrics/src/cow.ts:175), [build.ts:141](/Users/hodlmedia/agripinaa/packages/exec-metrics/src/receipt/build.ts:141), [exec.ts:84](/Users/hodlmedia/agripinaa/apps/web/src/lib/exec.ts:84) — **medium** — Attribution parses `fullAppData.appCode` without checking that its hash equals the signed `order.appData`; receipt generation does not gate on `isOphisOrder` at all. A hostile order API can attach `appCode:"ophis"` to a non-Ophis order or issue an Ophis-branded receipt for any UID. **Fix:** hash-bind appData, recompute/verify the order UID, validate `trade.orderUid`, and reject non-Ophis receipts.

14. [build.ts:124](/Users/hodlmedia/agripinaa/packages/exec-metrics/src/receipt/build.ts:124) — **medium** — Receipt surplus compares partial execution against the full signed limit. For a buy order with limit 100, executed buy 10 and executed sell 5, it reports 95% rather than the correct 50%. **Fix:** scale the signed limit by the filled fraction using bigint math, as `surplus.ts` already does.

15. [drill-hf.ts:30](/Users/hodlmedia/agripinaa/apps/agents/src/drill-hf.ts:30), [drill-hf.ts:53](/Users/hodlmedia/agripinaa/apps/agents/src/drill-hf.ts:53) — **medium** — The mainnet drill accepts an arbitrary borrow amount without enforcing a minimum projected HF. A typo can borrow to Aave’s limit and leave the wallet one small price move from liquidation before the next agent tick. **Fix:** hard-cap the amount, simulate/project post-borrow HF, require a safe floor, and default drills to a fork/testnet.

16. [route.ts:17](/Users/hodlmedia/agripinaa/apps/web/src/app/api/index/agents/route.ts:17), [merged.ts:78](/Users/hodlmedia/agripinaa/packages/agent-index/src/sources/merged.ts:78) — **low** — Negative limits are accepted. Under snapshot fallback, `limit=-1` becomes `slice(0,-1)`, bypassing the 100-item cap; arbitrary cursors also create cache/upstream-request amplification. **Fix:** require `limit` in `[1,100]` and a bounded positive cursor.

Verified: the production scope builder rejects empty allowlists and token-pins USDT/USDC using 18 decimals; Ophis approvals are amount-bounded, receiver-pinned, and expire after 30 minutes; the 8004scan key remains server-only. The requested `apps/agents/contracts/TestUSD.sol` does not exist; the actual [TestUSD.sol](/Users/hodlmedia/agripinaa/packages/spikes/contracts/TestUSD.sol:1) has no material issue worse than its intentional public mint. No files were modified.
