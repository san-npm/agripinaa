diff --git a/dist/core/internal/schema/request.js b/dist/core/internal/schema/request.js
index 68844e473d5ce562796b9ea54c5bd262eba4fff6..24d44b30e4a4b36de2b792c70b1670494746315a 100644
--- a/dist/core/internal/schema/request.js
+++ b/dist/core/internal/schema/request.js
@@ -51,7 +51,7 @@ export function validate(schema, value) {
 }
 /** @internal */
 export const schemaWithJsonRpc = (schema) => z.intersection(schema, z.object({
-    _returnType: z.unknown(),
+    _returnType: z.optional(z.unknown()),
     id: z.number(),
     jsonrpc: z.literal('2.0'),
 }));
diff --git a/dist/server/Route.d.ts b/dist/server/Route.d.ts
index 85f8dc872ef09991b0c89db4e66421fbd045761a..23fa34822478ecd1d5f0d8896396dc4f2fbc7bcb 100644
--- a/dist/server/Route.d.ts
+++ b/dist/server/Route.d.ts
@@ -81,6 +81,8 @@ export declare namespace merchant {
         });
         /** Whether to sponsor calls or not, and the condition to do so. */
         sponsor?: boolean | ((request: MerchantSchema.wallet_prepareCalls.Parameters) => MaybePromise<boolean>) | undefined;
+        /** Final policy check after the relay returns the exact signed fee quote. */
+        approve?: ((request: MerchantSchema.wallet_prepareCalls.Parameters, result: MerchantSchema.wallet_prepareCalls.Response) => MaybePromise<boolean>) | undefined;
         /** Relay transport override. */
         relay?: Porto.Config['relay'] | undefined;
     };
diff --git a/dist/server/Route.js b/dist/server/Route.js
index 310b7a4824e716b61f6b667a92091a6780e7bc67..ee25c55ed22b6d17afe766e814dfa373bf2e2853 100644
--- a/dist/server/Route.js
+++ b/dist/server/Route.js
@@ -3,6 +3,7 @@ import { cors } from 'hono/cors';
 import { poweredBy } from 'hono/powered-by';
 import * as RpcResponse from 'ox/RpcResponse';
 import { createClient, rpcSchema } from 'viem';
+import { privateKeyToAccount } from 'viem/accounts';
 import * as z from 'zod/mini';
 import * as Porto from '../core/Porto.js';
 import * as Key from '../viem/Key.js';
@@ -116,14 +117,14 @@ export function merchant(options) {
         }
         switch (request.method) {
             case 'wallet_prepareCalls': {
-                const sponsor = (() => {
-                    if (typeof options.sponsor === 'function')
-                        return options.sponsor(request.params[0]);
-                    if (typeof options.sponsor === 'boolean')
-                        return options.sponsor;
-                    return true;
-                })();
                 try {
+                    const sponsor = await (() => {
+                        if (typeof options.sponsor === 'function')
+                            return options.sponsor(request._decoded.params[0]);
+                        if (typeof options.sponsor === 'boolean')
+                            return options.sponsor;
+                        return true;
+                    })();
                     const result = await client.request({
                         ...request,
                         params: [
@@ -143,7 +144,11 @@ export function merchant(options) {
                             },
                         ],
                     });
-                    const { capabilities } = z.decode(MerchantSchema.wallet_prepareCalls.Response, result);
+                    const decoded = z.decode(MerchantSchema.wallet_prepareCalls.Response, result);
+                    const { capabilities } = decoded;
+                    if (sponsor && options.approve &&
+                        !(await options.approve(request._decoded.params[0], decoded)))
+                        throw new Error('merchant rejected relay quote');
                     const { feePayerDigest } = capabilities;
                     if (!feePayerDigest)
                         throw new Error('`feePayerDigest` is required');
@@ -151,6 +156,9 @@ export function merchant(options) {
                         ? await Key.sign(key, {
                             address: null,
                             payload: feePayerDigest,
+                            // EOA owners do not need a registered-key wrapper.
+                            wrap: typeof options.key !== 'string' ||
+                                privateKeyToAccount(options.key).address.toLowerCase() !== address.toLowerCase(),
                         })
                         : undefined;
                     const response = RpcResponse.from({
diff --git a/dist/viem/RelayActions.js b/dist/viem/RelayActions.js
index d8616ce7adc468345111e36b75c7582dd8c5f965..835db70bf3f21498afb5b2bd89376b9c56c3a45f 100644
--- a/dist/viem/RelayActions.js
+++ b/dist/viem/RelayActions.js
@@ -122,11 +122,7 @@ export async function prepareCalls(client, parameters) {
                 transport: http(merchantUrl),
             });
             // Prepare with Merchant RPC.
-            return await RelayActions.prepareCalls(client_, args).catch((e) => {
-                console.error(e);
-                // Fall back to default client.
-                return RelayActions.prepareCalls(client, args);
-            });
+            return await RelayActions.prepareCalls(client_, args);
         }
         return await RelayActions.prepareCalls(client, args);
     })();
diff --git a/src/core/internal/schema/request.ts b/src/core/internal/schema/request.ts
index ac5f65a7c3ffffce3b614080b3f6508cb645ac9b..64659032cb2e7610902ff0f8f57cd2b9f02e82fc 100644
--- a/src/core/internal/schema/request.ts
+++ b/src/core/internal/schema/request.ts
@@ -84,7 +84,7 @@ export const schemaWithJsonRpc = <schema extends z.ZodMiniType>(
   z.intersection(
     schema,
     z.object({
-      _returnType: z.unknown(),
+      _returnType: z.optional(z.unknown()),
       id: z.number(),
       jsonrpc: z.literal('2.0'),
     }),
diff --git a/src/server/Route.ts b/src/server/Route.ts
index bcbc2d9e4eed9bbd8668512ee01215bf3fed0858..44a0737ca5c66c067c5543a43d251ae155ac76dc 100644
--- a/src/server/Route.ts
+++ b/src/server/Route.ts
@@ -6,6 +6,7 @@ import type * as Address from 'ox/Address'
 import type * as Hex from 'ox/Hex'
 import * as RpcResponse from 'ox/RpcResponse'
 import { createClient, rpcSchema } from 'viem'
+import { privateKeyToAccount } from 'viem/accounts'
 import * as z from 'zod/mini'
 import type * as RpcSchema_relay from '../core/internal/relay/rpcSchema.js'
 import type { MaybePromise, OneOf } from '../core/internal/types.js'
@@ -154,14 +155,13 @@ export function merchant(options: merchant.Options) {
 
     switch (request.method) {
       case 'wallet_prepareCalls': {
-        const sponsor = (() => {
-          if (typeof options.sponsor === 'function')
-            return options.sponsor(request.params[0]! as never)
-          if (typeof options.sponsor === 'boolean') return options.sponsor
-          return true
-        })()
-
         try {
+          const sponsor = await (() => {
+            if (typeof options.sponsor === 'function')
+              return options.sponsor(request._decoded.params[0]! as never)
+            if (typeof options.sponsor === 'boolean') return options.sponsor
+            return true
+          })()
           const result = await client.request({
             ...request,
             params: [
@@ -181,10 +181,16 @@ export function merchant(options: merchant.Options) {
               },
             ],
           })
-          const { capabilities } = z.decode(
+          const decoded = z.decode(
             MerchantSchema.wallet_prepareCalls.Response,
             result,
           )
+          const { capabilities } = decoded
+          if (
+            sponsor &&
+            options.approve &&
+            !(await options.approve(request._decoded.params[0]! as never, decoded))
+          ) throw new Error('merchant rejected relay quote')
           const { feePayerDigest } = capabilities
           if (!feePayerDigest) throw new Error('`feePayerDigest` is required')
 
@@ -192,6 +198,10 @@ export function merchant(options: merchant.Options) {
             ? await Key.sign(key, {
                 address: null,
                 payload: feePayerDigest,
+                // An EOA owner signs natively; wrapping would require its key
+                // to be authorized before the first fee payment can execute.
+                wrap: typeof options.key !== 'string' ||
+                  privateKeyToAccount(options.key).address.toLowerCase() !== address.toLowerCase(),
               })
             : undefined
 
@@ -253,6 +263,13 @@ export declare namespace merchant {
           request: MerchantSchema.wallet_prepareCalls.Parameters,
         ) => MaybePromise<boolean>)
       | undefined
+    /** Final policy check after the relay returns the exact signed fee quote. */
+    approve?:
+      | ((
+          request: MerchantSchema.wallet_prepareCalls.Parameters,
+          result: MerchantSchema.wallet_prepareCalls.Response,
+        ) => MaybePromise<boolean>)
+      | undefined
     /** Relay transport override. */
     relay?: Porto.Config['relay'] | undefined
   }
diff --git a/src/viem/RelayActions.ts b/src/viem/RelayActions.ts
index 81613fab3c3dee6b0000ecd38d0dd10b0e086e71..1361466d93d8cfe388eba1d8747cec9225fcde23 100644
--- a/src/viem/RelayActions.ts
+++ b/src/viem/RelayActions.ts
@@ -270,11 +270,7 @@ export async function prepareCalls<
         transport: http(merchantUrl),
       })
       // Prepare with Merchant RPC.
-      return await RelayActions.prepareCalls(client_, args).catch((e) => {
-        console.error(e)
-        // Fall back to default client.
-        return RelayActions.prepareCalls(client, args)
-      })
+      return await RelayActions.prepareCalls(client_, args)
     }
     return await RelayActions.prepareCalls(client, args)
   })()
